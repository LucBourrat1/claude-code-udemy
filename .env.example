# Required — generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=

# Optional — defaults to http://localhost:3000
BETTER_AUTH_URL=

# Optional — defaults to http://localhost:3000
NEXT_PUBLIC_APP_URL=
